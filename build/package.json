{
  "name": "julespatry-js",
  "version": "0.0.2",
  "description": "A library of server/client agnostic javascript functions",
  "main": "./index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node delete-build-folder && tsc --outDir ./build && node copy-packagejson"
  },
  "author": "Jules Patry",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.11.3",
    "typescript": "^4.4.4"
  }
}
