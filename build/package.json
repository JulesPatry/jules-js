{
  "name": "jules-js",
  "version": "0.0.2",
  "description": "A library of server/client agnostic javascript functions",
  "main": "./index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./build-pre && tsc --outDir ./build && node ./build-post",
    "pub": "npm run build && cd ./build && npm publish && cd .."
  },
  "author": "Jules Patry",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.3",
    "typescript": "^4.4.4"
  }
}
